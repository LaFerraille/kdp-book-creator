target-version = "py311"
line-length = 100
extend-exclude = ["wiki", "build"]

[lint]
select = ["E", "F", "I", "B", "UP"]
# LaTeX templates are full of braces; %-formatting keeps them readable.
ignore = ["UP031"]
