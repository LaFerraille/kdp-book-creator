[pytest]
testpaths = tests
# Network tests hit KDP's live site. Excluded by default: a suite that fails
# when Amazon is slow is a suite people learn to ignore. Run with `-m network`.
addopts = -m 'not network'
markers =
    network: drives KDP's live website
